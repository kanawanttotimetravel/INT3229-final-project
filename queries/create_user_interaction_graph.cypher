CALL gds.graph.project(
  'userGraph',                      // Tên của graph projection
  'User',                            // Nút (node) loại User
  'InteractsWith'                    // Mối quan hệ (relationship) loại InteractsWith
);
